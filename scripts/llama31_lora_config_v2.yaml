model_id: meta-llama/Meta-Llama-3.1-8B-Instruct

dataset_path: data/cleaned/newsroom_lora_v2.jsonl
output_dir: outputs/llama31_lora_v2

max_seq_len: 512

# keep these aligned with what worked for v1, example:
batch_size: 2
gradient_accumulation_steps: 8
learning_rate: 1e-4
num_epochs: 1

lora_r: 16
lora_alpha: 32
lora_dropout: 0.05
target_modules:
  - q_proj
  - k_proj
  - v_proj
  - o_proj
